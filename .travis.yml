sudo: required
language: go
go:
  - 1.9
env:
  global:
    - CGO_ENABLED=0
   matrix:
    - SERVICE=eventservice
    - SERVICE=bookingservice
script:
  - go vet $(go list ./... | grep -v vendor)
  - cd src/${SERVICE} && go build